Put your optimized 3D models here.

Supported:
- .glb / .gltf (recommended)
- .obj

Tips:
- Keep each model < 5MB (ideally 0.5–2MB)
- Apply Draco / Meshopt compression if possible
- Use 1K textures and WebP where applicable
- Make sure model scale is correct (mm → meters conversion if needed)

Example paths used in the UI (replace with real files):
- /models/sample.glb

